diffusers>=0.33
transformers>=4.48
accelerate
safetensors
gradio>=5
pillow
# compel (プロンプトの重み付き構文と 77 トークン超の解釈) が実際に使う唯一の依存。
# compel 本体は notebook (Jupyter 一式) を依存に宣言しているが実行時には読まないため、
# setup.bat 側で --no-deps を付けて入れている
pyparsing
